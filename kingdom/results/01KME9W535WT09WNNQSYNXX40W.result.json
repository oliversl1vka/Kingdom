{
  "job_id": "01KME9W535WT09WNNQSYNXX40W",
  "success": true,
  "content": "diff --git a/packages/ui/src/engine/pixel-characters.ts b/packages/ui/src/engine/pixel-characters.ts\nindex 3f2a1d7d..c9e1b2f4 100644\n--- a/packages/ui/src/engine/pixel-characters.ts\n+++ b/packages/ui/src/engine/pixel-characters.ts\n@@ -1430,49 +1430,97 @@\n-        const x = bounds.x + Math.random() * bounds.w;\n-        const y = bounds.y + Math.random() * bounds.h;\n-        const vx = (Math.random() - 0.5) * 0.05;\n-        const vy = (Math.random() - 0.5) * 0.02;\n-        const color = `rgba(200, 180, 150, 0.6)`;\n-        const size = 1;\n-        const life = 80 + Math.floor(Math.random() * 40);\n-        spawnParticle(id, x, y, color, vx, vy, size, life);\n-      }\n-      break;\n-    }\n-    case 'Dungeon': {\n-      // Dripping water droplets falling\n-      if (particles.length < 25 && Math.random() < 0.2) {\n-        const x = bounds.x + Math.random() * bounds.w;\n-        const y = bounds.y + 2;\n-        const vx = 0;\n-        const vy = 0.7 + Math.random() * 0.3;\n-        const color = `rgba(180, 220, 255, 0.8)`;\n-        const size = 1 + Math.random();\n-        const life = 50 + Math.floor(Math.random() * 20);\n-        spawnParticle(id, x, y, color, vx, vy, size, life);\n-      }\n-      break;\n-    }\n-    case 'Watchtower': {\n-      // Wind streaks moving horizontally\n-      if (particles.length < 20 && Math.random() < 0.25) {\n-        const y = bounds.y + Math.random() * bounds.h;\n-        const x = bounds.x + (Math.random() < 0.5 ? bounds.w + 2 : -2);\n-        const vx = (x < bounds.x) ? 0.5 + Math.random() * 0.3 : -0.5 - Math.random() * 0.3;\n+        switch (id) {\n+          case 'Workshop': {\n+            // Orange forge sparks rising and fading\n+            if (particles.length < 30 && Math.random() < 0.3) {\n+              const x = bounds.x + Math.random() * bounds.w;\n+              const y = bounds.y + bounds.h - 5 + Math.random() * 5;\n+              const vx = (Math.random() - 0.5) * 0.1;\n+              const vy = -0.3 - Math.random() * 0.3;\n+              const color = `rgba(255, 140, 0, ${0.4 + Math.random() * 0.6})`; // orange\n+              const size = 1 + Math.random();\n+              const life = 40 + Math.floor(Math.random() * 30);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+          case 'Healing Chamber': {\n+            // Green healing sparkles floating upward\n+            if (particles.length < 25 && Math.random() < 0.25) {\n+              const x = bounds.x + Math.random() * bounds.w;\n+              const y = bounds.y + bounds.h - 5 + Math.random() * 5;\n+              const vx = (Math.random() - 0.5) * 0.05;\n+              const vy = -0.2 - Math.random() * 0.2;\n+              const color = `rgba(50, 205, 50, ${0.3 + Math.random() * 0.5})`; // lime green\n+              const size = 1;\n+              const life = 50 + Math.floor(Math.random() * 40);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+          case 'Library': {\n+            // Dust motes drifting slowly\n+            if (particles.length < 20 && Math.random() < 0.2) {\n+              const x = bounds.x + Math.random() * bounds.w;\n+              const y = bounds.y + Math.random() * bounds.h;\n+              const vx = (Math.random() - 0.5) * 0.02;\n+              const vy = (Math.random() - 0.5) * 0.01;\n+              const color = `rgba(200, 180, 150, 0.4)`; // pale beige dust\n+              const size = 1;\n+              const life = 80 + Math.floor(Math.random() * 40);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+          case 'Dungeon': {\n+            // Dripping water droplets falling\n+            if (particles.length < 25 && Math.random() < 0.2) {\n+              const x = bounds.x + Math.random() * bounds.w;\n+              const y = bounds.y + 2;\n+              const vx = 0;\n+              const vy = 0.7 + Math.random() * 0.3;\n+              const color = `rgba(180, 220, 255, 0.8)`; // light blue\n+              const size = 1 + Math.random();\n+              const life = 50 + Math.floor(Math.random() * 20);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+          case 'Watchtower': {\n+            // Wind streaks moving horizontally\n+            if (particles.length < 20 && Math.random() < 0.25) {\n+              const y = bounds.y + Math.random() * bounds.h;\n+              const x = bounds.x + (Math.random() < 0.5 ? bounds.w + 2 : -2);\n+              const vx = (x < bounds.x) ? 0.5 + Math.random() * 0.3 : -0.5 - Math.random() * 0.3;\n+              const vy = 0;\n+              const color = `rgba(220, 220, 255, 0.3)`; // pale blue wind streaks\n+              const size = 2;\n+              const life = 40 + Math.floor(Math.random() * 30);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+          default: {\n+            // Default ambient particles (dust motes)\n+            if (particles.length < 20 && Math.random() < 0.1) {\n+              const x = bounds.x + Math.random() * bounds.w;\n+              const y = bounds.y + Math.random() * bounds.h;\n+              const vx = (Math.random() - 0.5) * 0.05;\n+              const vy = (Math.random() - 0.5) * 0.02;\n+              const color = `rgba(200, 180, 150, 0.6)`;\n+              const size = 1;\n+              const life = 80 + Math.floor(Math.random() * 40);\n+              spawnParticle(id, x, y, color, vx, vy, size, life);\n+            }\n+            break;\n+          }\n+        }\n       }\n-      break;\n-    }\n-    case 'Watchtower': {\n-      // Wind streaks moving horizontally\n-      if (particles.length < 20 && Math.random() < 0.25) {\n-        const y = bounds.y + Math.random() * bounds.h;\n-        const x = bounds.x + (Math.random() < 0.5 ? bounds.w + 2 : -2);\n-        const vx = (x < bounds.x) ? 0.5 + Math.random() * 0.3 : -0.5 - Math.random() * 0.3;\n+      \n+",
  "tokens_used": 4358,
  "finish_reason": "stop"
}