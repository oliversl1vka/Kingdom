{
  "job_id": "01KME8JESM4ZJDCPYM1C4F96M8",
  "success": true,
  "content": "--- a/packages/ui/src/engine/pixel-characters.ts\n+++ b/packages/ui/src/engine/pixel-characters.ts\n@@ -590,13 +590,88 @@\n-// ─── Character movement ────────────────────────────────────────────\n-\n-interface CharState { x: number; y: number; tx: number; ty: number; dir: string; wf: number; moving: boolean; idle: number }\n-const charStates = new Map<string, CharState>();\n-\n-function getCS(id: string, sx: number, sy: number): CharState {\n-  if (!charStates.has(id)) charStates.set(id, { x: sx, y: sy, tx: sx, ty: sy, dir: 'down', wf: 0, moving: false, idle: Math.random() * 200 });\n-  return charStates.get(id)!;\n-}\n-\n-function updateMovement(cs: CharState, bounds: { x: number; y: number; w: number; h: number }) {\n-  const dx = cs.tx - cs.x, dy = cs.ty - cs.y;\n-  const dist = Math.sqrt(dx * dx + dy * dy);\n-  if (dist > 2) {\n-    cs.moving = true;\n-    cs.x += (dx / dist) * 0.4;\n-    cs.y += (dy / dist) * 0.4;\n-    cs.wf++;\n-    cs.dir = Math.abs(dx) > Math.abs(dy) ? (dx > 0 ? 'right' : 'left') : (dy > 0 ? 'down' : 'up');\n-  } else {\n-    cs.moving = false; cs.wf = 0; cs.idle--;\n-    if (cs.idle <= 0) {\n+// ─── Character movement and walk animation ─────────────────────────────\n+\n+interface CharState {\n+  x: number;\n+  y: number;\n+  tx: number;\n+  ty: number;\n+  dir: 'up' | 'down' | 'left' | 'right';\n+  wf: number; // walk frame counter\n+  moving: boolean;\n+  idle: number;\n+}\n+\n+const charStates = new Map<string, CharState>();\n+\n+function getCS(id: string, sx: number, sy: number): CharState {\n+  if (!charStates.has(id)) {\n+    charStates.set(id, {\n+      x: sx,\n+      y: sy,\n+      tx: sx,\n+      ty: sy,\n+      dir: 'down',\n+      wf: 0,\n+      moving: false,\n+      idle: Math.random() * 200,\n+    });\n+  }\n+  return charStates.get(id)!;\n+}\n+\n+function updateMovement(cs: CharState, bounds: { x: number; y: number; w: number; h: number }) {\n+  const dx = cs.tx - cs.x,\n+    dy = cs.ty - cs.y;\n+  const dist = Math.sqrt(dx * dx + dy * dy);\n+  if (dist > 2) {\n+    cs.moving = true;\n+    cs.x += (dx / dist) * 0.4;\n+    cs.y += (dy / dist) * 0.4;\n+    cs.wf++;\n+    // Loop walk frame counter every 4 frames for smooth animation\n+    if (cs.wf >= 4) cs.wf = 0;\n+    cs.dir =\n+      Math.abs(dx) > Math.abs(dy)\n+        ? dx > 0\n+          ? 'right'\n+          : 'left'\n+        : dy > 0\n+        ? 'down'\n+        : 'up';\n+  } else {\n+    cs.moving = false;\n+    cs.wf = 0;\n+    cs.idle--;\n+    if (cs.idle <= 0) {\n@@ -895,6 +960,74 @@\n-}\n-\n-// ──────────────────────────────────────────────────────────────────────\n+}\n+\n+// Draw character with walk cycle animation\n+function drawCharacter(\n+  ctx: CanvasRenderingContext2D,\n+  id: string,\n+  x: number,\n+  y: number,\n+  tier: CharacterType,\n+  dir: 'up' | 'down' | 'left' | 'right',\n+  wf: number,\n+  moving: boolean,\n+  animState: AnimState | null = null\n+) {\n+  // Base character draw (head, body, arms)\n+  // ... existing drawing code here (not shown) ...\n+\n+  // Draw legs with 4-frame walk cycle animation\n+  // Each frame moves legs alternately forward/backward\n+  // Leg offset pattern for frames 0-3:\n+  // frame 0: left leg forward, right leg back\n+  // frame 1: legs neutral\n+  // frame 2: left leg back, right leg forward\n+  // frame 3: legs neutral\n+\n+  // Determine leg offsets based on frame\n+  const legOffset = [1, 0, -1, 0];\n+  const frame = wf % 4;\n+  let leftLegOffset = 0;\n+  let rightLegOffset = 0;\n+  if (moving) {\n+    leftLegOffset = legOffset[frame];\n+    rightLegOffset = -legOffset[frame];\n+  }\n+\n+  ctx.save();\n+  ctx.translate(x, y);\n+\n+  // Draw legs depending on direction\n+  // Assume legs are rectangles or simple shapes drawn relative to character base\n+  // Adjust leg positions for walking animation\n+\n+  // Example leg drawing:\n+  ctx.fillStyle = 'black'; // leg color placeholder\n+\n+  switch (dir) {\n+    case 'down':\n+      // Left leg\n+      ctx.fillRect(-3, 10 + leftLegOffset, 4, 8);\n+      // Right leg\n+      ctx.fillRect(1, 10 + rightLegOffset, 4, 8);\n+      break;\n+    case 'up':\n+      ctx.fillRect(-3, 6 + leftLegOffset, 4, 8);\n+      ctx.fillRect(1, 6 + rightLegOffset, 4, 8);\n+      break;\n+    case 'left':\n+      ctx.fillRect(-4 + leftLegOffset, 8, 4, 8);\n+      ctx.fillRect(-4 + rightLegOffset, 14, 4, 8);\n+      break;\n+    case 'right':\n+      ctx.fillRect(0 + leftLegOffset, 8, 4, 8);\n+      ctx.fillRect(0 + rightLegOffset, 14, 4, 8);\n+      break;\n+  }\n+\n+  ctx.restore();\n+}\n+\n+export { getCS, updateMovement, drawCharacter };\n--- a/packages/ui/src/scenes/agents.tsx\n+++ b/packages/ui/src/scenes/agents.tsx\n@@ -1,20 +1,27 @@\n-import React, { useRef, useEffect, useState } from 'react';\r\n-import { syncCanvasSize, clearCanvas, drawText, startGameLoop } from '../engine/renderer.js';\r\n-import { COLORS, getStateColor, type AgentState } from '../assets/sprites.js';\r\n-import { drawCharacter, drawRoom, getCS, updateMovement, getWorkstationPosition, updateCharacterPosition, type CharacterType, type AnimState } from '../engine/pixel-characters.js';\r\n+import React, { useRef, useEffect, useState } from 'react';\r\n+import {\r\n+  syncCanvasSize,\r\n+  clearCanvas,\r\n+  drawText,\r\n+  startGameLoop,\r\n+} from '../engine/renderer.js';\r\n+import {\r\n+  COLORS,\r\n+  getStateColor,\r\n+  type AgentState,\r\n+} from '../assets/sprites.js';\r\n+import {\r\n+  drawCharacter,\r\n+  drawRoom,\r\n+  getCS,\r\n+  updateMovement,\r\n+  getWorkstationPosition,\r\n+  updateCharacterPosition,\r\n+  type CharacterType,\r\n+  type AnimState,\r\n+} from '../engine/pixel-characters.js';\r\n \r\n interface AgentInfo {\r\n   id: string;\r\n@@ -51,10 +58,30 @@\n-  // Poll agents every 5 seconds\r\n-  useEffect(() => {\r\n+  // Poll agents every 5 seconds\r\n+  useEffect(() => {\r\n+    const canvas = canvasRef.current;\r\n+    if (!canvas) return;\r\n+    const ctx = canvas.getContext('2d');\r\n+    if (!ctx) return;\n+\n+    syncCanvasSize(canvas);\n+\n+    let animationFrameId: number;\n+\n+    function tick() {\n+      clearCanvas(ctx);\n+\n+      // Draw rooms and agents\n+      for (const agent of agents) {\n+        // Get character state and update movement\n+        const cs = getCS(agent.id, 50, 50); // Starting position placeholder\n+        updateMovement(cs, { x: 0, y: 0, w: canvas.width, h: canvas.height });\n+\n+        // Draw character with walk animation frames\n+        drawCharacter(ctx, agent.id, cs.x, cs.y, agent.tier, cs.dir, cs.wf, cs.moving);\n+      }\n+\n+      animationFrameId = requestAnimationFrame(tick);\n+    }\n+\n+    animationFrameId = requestAnimationFrame(tick);\n+\n+    return () => cancelAnimationFrame(animationFrameId);\n+  }, [agents]);\n--- END PATCH ---",
  "tokens_used": 4681,
  "finish_reason": "stop"
}